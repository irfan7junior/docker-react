language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t irfan7junior/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true irfan7junior/docker-react npm run test
